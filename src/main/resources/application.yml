spring:
  application:
    name: zz-server



  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:zz-server;MODE=PostgreSQL

    username: zz-server
    password: ENC(LBAe1oBu93ZFtB5zj0uNO8ZQ+O/dzTm/)

  h2:
    console:
      enabled: true
      path: /zz-server/h2-console
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  expiration: 86400000
  secret: 
server:
  port: 9090
